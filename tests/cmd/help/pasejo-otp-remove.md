```
$ pasejo otp help remove
Remove a one-time password

Usage: pasejo otp remove [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -f, --force          Delete an existing one-time password without prompting
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```

```
$ pasejo otp remove --help
Remove a one-time password

Usage: pasejo otp remove [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -f, --force          Delete an existing one-time password without prompting
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```

```
$ pasejo otp remove -h
Remove a one-time password

Usage: pasejo otp remove [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -f, --force          Delete an existing one-time password without prompting
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```
