```
$ pasejo otp help show
Show a one-time password

Usage: pasejo otp show [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -c, --clip           Copy one-time password to clipboard
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```

```
$ pasejo otp show --help
Show a one-time password

Usage: pasejo otp show [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -c, --clip           Copy one-time password to clipboard
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```

```
$ pasejo otp show -h
Show a one-time password

Usage: pasejo otp show [OPTIONS] <PASSWORD_PATH>

Arguments:
  <PASSWORD_PATH>  The path of the one-time password within the selected store

Options:
  -s, --store <STORE>  Optional name of store to use. Defaults to the default store or the first one defined in the local user configuration
  -c, --clip           Copy one-time password to clipboard
  -v, --verbose...     Increase logging verbosity
  -q, --quiet...       Decrease logging verbosity
  -h, --help           Print help

```
