```
$ pasejo store add --path store --name Personal
Store 'Personal' added at '[CWD]/store'

$ pasejo store set-default personal
Store 'Personal' is now the default

$ pasejo store set-default PERSONAL
Store 'Personal' is now the default

```
