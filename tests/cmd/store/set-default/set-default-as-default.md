```
$ pasejo store add --path store --name something --default
Store 'something' added at '[CWD]/store'
Store 'something' is now the default

$ pasejo store set-default something
Store 'something' is now the default

```
